kafka分区数一定要匹配flink的并行度
一个分区的数据只会发个给一个并行度
如果kafka分区只有1，flink设置并行度为3，那么只会有一个并行度有数据，其他并行度空闲，
关键的来了：指定水位线策略后没有数据的并行实例也会向下游发送watermark，是Long的最小值
到后面开窗计算时要以上游最小的watermark为准，导致始终无法触发计算

结论：为某个流分配水位线之前一定要保证该流的所有并行实例均有数据，
	 该流之前所有并行实例是否有数据是无关紧要的，只要保证当前分配水位线策略的流所有并行实例有数据即可（如制定重分区策略）